# data

이 폴더는 실습 데이터를 넣는 위치다.

- `pdfs/` : PDF 파일 저장
- `sample.db` : SQLite 샘플 DB
- `sample-report.pdf` : 강의용 샘플 PDF
- `sample-report.txt` : PDF 원문 참고용 텍스트

강의에서는 먼저 샘플 PDF 내용을 보여주고,
그 다음 DB row와 함께 검색 대상에 들어가는 흐름을 설명하면 이해가 쉽다.
